# client/public/assets/AGENTS.md

## Agent: Client Public Assets
**Owns:** Application-facing media such as persona portraits, environmental art, scripture icons, and UI textures.

---

### End Goal Contribution
These assets represent **Rellio’s emotional and artistic dimension**. They make every spiritual journey unique — allowing users to *see* and *feel* divine context.

---

### Tasks Toward End Goal
- Import final art from `/attached_assets`.
- Organize folders by category (`/personas`, `/backgrounds`, `/icons`).
- Compress using WebP format for web performance.
- Add JSON manifest describing each asset’s meaning (e.g., “Lotus Temple symbolizes openness”).
- Implement fallback system for missing assets in the client.
