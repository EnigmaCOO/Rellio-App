You are a senior full-stack engineer tasked with debugging and enhancing the Universal Wisdom Explorer voice system in Rellio to make the click-to-speak button functional and achieve a Grok-like interactive, easy-to-use, interruptible experience, without breaking non-voice functionality or logic.

Do not alter routing, APIs, state schemas, or IDs used by analytics/tests.
Keep all existing component props and exported names.
Modify only voice-related code in <VoiceFirstChatInterface.tsx>, <VoiceModeHandler.tsx>, and related files to fix the button issue and add Grok-like features.
Write accessible, responsive, and performant code.
Prefer Tailwind CSS for any UI tweaks.
Use semantic HTML and ARIA where relevant.
Produce a single PR with clear commits (e.g., "fix(voice): enable click-to-speak button", "feat(voice): add Grok-like interruption and ease-of-use").
Test for button functionality, interruptions, and seamless flow; log detailed errors for debugging.

TASK BRIEF (paste after system block)
Goal: Debug and fix the click-to-speak button in the Universal Wisdom Explorer chat (client/src/components/chat/VoiceFirstChatInterface.tsx) that currently does nothing when pressed, then enhance the voice system to be Grok-like: interactive (continuous listening with easy toggling), user-friendly (permission prompts, visual feedback), interruptible (detect and handle user speech during AI playback), and seamless (auto-send, echo cancellation, context retention). Analyze why the button isn't working (likely permission issues, browser incompatibility, event prevention in toggleListening, or state conflicts in useVoiceModeHandler) and ensure a mesmerizing, pioneer-level experience for spiritual conversations in the right-hand chat column.
Key Features and Goals:

Debug Click-to-Speak Button Failure:

Investigate <VoiceFirstChatInterface.tsx> button onClick (toggleListening from useVoiceModeHandler): Add logging for state changes, permissions, and errors (e.g., console.log in startListening/stopListening).
Handle Common Causes: Check Web Speech API support (Chrome/Edge required); prompt for mic permission if denied (navigator.mediaDevices.getUserMedia); prevent event bubbling (e.stopPropagation()); resolve state conflicts (e.g., if disabled or isListening stuck).
Add UI Feedback: If not supported/ permitted, show toast ("Voice not supported—use Chrome/Edge" or "Mic permission denied—enable in browser settings").


Grok-Like Interactivity and Ease-of-Use:

Toggle Behavior: Button starts continuous listening (continuous=true, interimResults=true); auto-send after 1.5s pause (onspeechend with confidence >0.85); visual waveform (teal bars pulsing) during input.
User-Friendly Prompts: On first click, show permission modal if needed; add "Tap to speak" tooltip; auto-resume after interruption.
Seamless Flow: Auto-send queries without manual confirm; integrate with ElevenLabs for instant voiced responses (low-latency streaming).


Interruptible During AI Playback (Grok-Style):

Keep background SpeechRecognition active (low sensitivity 0.3) during ElevenLabs playback (voiceState='speaking').
On user speech detection (onspeechstart, confidence >0.7), pause ElevenLabs (fade-out 300ms), stop recognition briefly, append interrupted response to history, and process new query with context ("You interrupted—continuing on your new thought...").
Isolation: Mute mic gain (gainNode.gain.value=0) during playback; enhance echo cancellation (Web Audio API createMediaStreamDestination with echoCancellation=true).


UI Enhancements for Mesmerizing Experience:

Button: Glow teal on hover, pulse red during listening, flash gold on send.
Orb (24px header): Purple processing, teal listening, gold speaking, red interrupted.
Transcript: Show only user input (exclusive mic capture); no AI voice bleed.


Technical Fixes:

Cleanup: useEffect for recognition.stop() on unmount; clear timeouts on state change.
Logging: Add detailed console.logs for button clicks, state transitions, and errors.



Deliverables:

Updated <VoiceFirstChatInterface.tsx>: Fix button onClick, add prompts/toasts.
Updated <VoiceModeHandler.tsx>: Enhance interruption, isolation, auto-send.
Tailwind/CSS: Button/orb animations (animate-pulse, transition-colors).
State/Hooks: useState for permissionStatus; useEffect for permission checks, background listener.

Provide React/TypeScript code with Tailwind classes, and describe state/effects. Ensure a pioneer-level, seamless voice system that's interactive, interruptible, and mesmerizing for spiritual exploration