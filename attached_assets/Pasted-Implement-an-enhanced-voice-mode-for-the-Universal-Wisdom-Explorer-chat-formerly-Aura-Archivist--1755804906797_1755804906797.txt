Implement an enhanced voice mode for the “Universal Wisdom Explorer” chat (formerly "Aura Archivist") in the right-hand column of the three-column Scripture Reader page (Left: Navigation, Center: Verse Card, Right: Chat), making it seamless and conversational like Grok’s voice mode. Focus on auto-sending after pauses, real-time interruption handling with context retention, echo cancellation, and dynamic persona switching based on the selected book/religion. Use Web Speech API for input, ElevenLabs for output (matching scripture voices), and integrate with OpenAI/XAI for context-aware responses. Maintain the white-themed UI (#FFFFFF, rounded corners 12px, shadow-md) with teal #00D5FF accents, faint purple-to-gold gradient overlay (#4B0082 to #D4AF37, 10% opacity), and mandala art for vibrancy. Sync with verse selection and app state for personalized, addictive interactions.
Key Features and Goals:

Voice Input System (Grok-Like Auto-Send and Listening):

Upgrade the input bar to voice-first: Default glowing circular microphone button (teal #00D5FF, 48px, white mic icon, hover pulse scale-105).
Active: On tap, start continuous listening with Web Speech API (continuous=true, interimResults=true); show real-time waveform (teal bars pulsing like Grok's viz) with “Listening…” (14px dark-gray #1F2937).
Auto-Sending Q&A: Transcribe live; auto-send query after 1.5s pause (configurable 1-3s via settings) using onspeechend event and confidence scoring (>0.8); no manual send—process as complete thought.
Fallback: Toggle text input (keyboard icon); auto-switch if voice fails.


Real-Time Interruption and Context Retention (Grok/ChatGPT-Style):

Detect interruptions during AI response (text/voice): If user speaks/types mid-output, halt generation/playback (clear buffer, pause ElevenLabs), append interrupted text to history, and process new input.
Smart Detection: Use background audio monitoring (Web Speech API) for volume/pause thresholds (> background noise, <1s silence); confidence scoring to "know" natural breaks.
Context Maintenance: Retain full conversation history (useState array or session storage); reference prior turns in responses (e.g., "Building on your question about love..."); continue seamlessly post-interruption (e.g., "You interrupted—let's pivot to your new query").
Visual: Red orb flash in header (teal orb turns red, like Grok), "Interrupted" tooltip; resume button.


Echo Cancellation and Real-Time Talking Back:

Echo Cancellation: Integrate Web Audio API (createMediaStreamDestination with echoCancellation=true) to suppress feedback; prompt headphones if detected (toast notification like ChatGPT/Grok).
Real-Time Responses: Stream AI output (OpenAI/XAI Realtime API) for <500ms latency; voice auto-plays (ElevenLabs streaming, persona-specific tones); respond regardless of mode (voice/text interruption)—system "talks back" naturally, asking follow-ups (e.g., "What aspect intrigues you?").
Ensure continuity: Even after interruption, append new input to context for coherent flow.


Dynamic Persona Switching (Scholarly Personalities):

Outside books: Default to "Unique Scholarly Personality" (neutral, wise voice via ElevenLabs, general insights across faiths).
Inside books: Auto-switch persona based on religion (e.g., Christian Priest for Bible: warm, pastoral tone; Islamic Mufti/Scholar for Quran: authoritative, interpretive voice)—use app state (selected book from Verse Card) to trigger.
Personas stored in <ScholarPersonas.tsx>; selectable via header dropdown if override needed.
Voice Sync: ElevenLabs personas match scripture (e.g., resonant for Mufti); adjust prompts (e.g., "Respond as an Islamic Scholar").


UI and Animation Enhancements:

Pulsing orb in header (teal 24px, purple processing, gold responding, red interrupted—like Grok/ChatGPT indicators).
Fade-in (200ms) for messages; waveform during input; ElevenLabs playback shows speaker wave.
No cut-offs: Full-height scrolling chat; auto-scroll to latest.



Deliverables:

Updated <EnhancedAuraArchivist.tsx> / <RightColumnChat.tsx>: Include voice handler, persona switcher, interruption logic.
<voiceconversationmanager> sub-component: For auto-send, interruption detection, context storage.</voiceconversationmanager>
ElevenLabs/Web Speech/OpenAI/XAI setup: Hooks in <VoiceInputControls.tsx> and <AudioPlaybackButton.tsx> for streaming, echo cancellation.
Tailwind/CSS: Extend for orb (animate-pulse), waveform (keyframe), interruption flash (transition-colors).
State/Hooks: useState for conversationHistory (array), currentPersona, isInterrupted, audioState; useEffect for continuous recognition (onresult, onspeechend), API streaming, context injection in prompts.

Provide React JSX code with Tailwind classes for these components, and describe any new state or effects needed. Ensure a fascinating, scholarly voice experience where the Universal Wisdom Explorer maintains unique personality outside books, switches intelligently inside, and converses smartly with context, interruptions, and real-time responses.